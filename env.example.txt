# ============================================================
# Task Tracker — Environment Configuration Reference
# ============================================================
# Copy this file to .env and fill in the values for your environment.
# All variables without defaults MUST be set before starting the application.
#
# This file is used by:
#   - Docker Compose (PostgreSQL container and API container)
#   - FastAPI backend (via pydantic-settings / python-dotenv)
# ============================================================


# ------------------------------------------------------------
# PostgreSQL Database (Docker Compose)
# Only required when running via Docker Compose with the db service.
# Not needed for local SQLite development.
# ------------------------------------------------------------

POSTGRES_DB=taskdb
POSTGRES_USER=taskdb

# Generate a secure password for production:
#   openssl rand -base64 32
POSTGRES_PASSWORD=your_secure_password_here


# ------------------------------------------------------------
# Backend: Database Connection URL
# ------------------------------------------------------------

# SQLite (local development — no Docker required):
DATABASE_URL=sqlite+aiosqlite:///./taskdb.db

# PostgreSQL (Docker Compose / production):
# DATABASE_URL=postgresql+asyncpg://taskdb:your_secure_password_here@db:5432/taskdb


# ------------------------------------------------------------
# Backend: Application Settings
# ------------------------------------------------------------

# ENVIRONMENT: Controls runtime behaviour.
#   development — enables hot-reload, verbose logging, relaxed error handling
#   production  — strict error handling, optimised for performance
ENVIRONMENT=development

# DEBUG: Enables FastAPI debug mode (true/false).
#   true  — automatic API docs (/docs), detailed error pages, verbose output
#   false — recommended for production
DEBUG=true


# ------------------------------------------------------------
# Backend: CORS Allowed Origins
# ------------------------------------------------------------

# Comma-separated list of origins the browser is permitted to call the API from.
# Must be a JSON-style list of quoted strings.
#
# Local development (Vite dev server):
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
#
# Production example (replace with your actual domain):
# CORS_ORIGINS=["https://your-domain.com"]
#
# Note: when the frontend and API are served under the same domain via a reverse
# proxy (e.g. nginx + Traefik), the browser treats requests as same-origin and
# CORS is not enforced — this setting only matters when origins differ.


# ------------------------------------------------------------
# Deployment: Application Domain (Docker Compose / Traefik)
# ------------------------------------------------------------

# The public hostname Traefik uses to route HTTPS traffic to the frontend.
# Only required when deploying with the production compose.yml and Traefik.
APP_DOMAIN=your-domain.com
