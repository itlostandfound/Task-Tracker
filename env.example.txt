# Database Configuration
POSTGRES_DB=taskdb
POSTGRES_USER=taskdb
# POSTGRES_PASSWORD: Generate a secure random password for production:
#   openssl rand -base64 32
# For development, a simple password is fine. Change this value for security.
POSTGRES_PASSWORD=your_secure_password_here

# API Configuration
DATABASE_URL=postgresql+asyncpg://taskdb:your_secure_password_here@db:5432/taskdb

# Environment Configuration
# ENVIRONMENT: Set to 'development' for local testing or 'production' for deployed instances.
# - development: Enables hot-reload, verbose logging, and relaxed error handling
# - production: Disables hot-reload, strict error handling, optimized for performance
ENVIRONMENT=development

# Debug Configuration
# DEBUG: Boolean flag controlling FastAPI debug mode (true/false)
# - true: Enables automatic API documentation, detailed error pages, and verbose output
# - false: Disables debug features, recommended for production environments
DEBUG=true
